---
description: Check that swarm's runtime preconditions are met (git, tmux, claude, $TMUX, inside a git repo).
---

Run `bash "${CLAUDE_PLUGIN_ROOT}/scripts/swarm-doctor"` and report the output to the user. If any check fails, point them at the README's prerequisites section and stop.
